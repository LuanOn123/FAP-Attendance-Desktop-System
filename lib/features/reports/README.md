# Member 4 integration

Add live/history/report screens and typed repositories here.
Extend the backend with lecturer-scoped reads and attendance-specific writes.
The core currently permits only lecturer profile, own schedules and own classes.
Avoid exposing unfiltered Students/Attendance via a generic getRows action.
